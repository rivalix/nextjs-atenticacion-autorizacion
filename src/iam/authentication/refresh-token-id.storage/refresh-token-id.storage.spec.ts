import { RefreshTokenIdStorage } from './refresh-token-id.storage';

describe('RefreshTokenIdStorage', () => {
  it('should be defined', () => {
    expect(new RefreshTokenIdStorage()).toBeDefined();
  });
});
